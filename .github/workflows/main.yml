name: Crypto Signal Bot

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Get code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install libraries
        run: pip install -r requirements.txt

      - name: Run Bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHANNEL_ID: ${{ secrets.CHANNEL_ID }}
        run: |
          echo "=== Starting Bot at $(date) ==="
          python bot.py
          echo "=== Bot Finished at $(date) ==="

      - name: Keep Alive - Update last run timestamp
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          echo "Last run: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" > .last-run
          git add .last-run
          git commit -m "Keep alive - Bot run at $(date -u +"%Y-%m-%d %H:%M:%S UTC")" || echo "Nothing to commit"
          git push || echo "Push failed"

      - name: Commit signal counter
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add signal_counter_15m.txt 2>/dev/null || true
          git diff --cached --quiet || git commit -m "Update signal counter [$(date -u +"%Y-%m-%d %H:%M:%S UTC")]"
          git push || echo "Nothing to commit"
